diff --git a/core/modules/package_manager/src/ProjectInfo.php b/core/modules/package_manager/src/ProjectInfo.php
index 1234567890..abcdefghij 100644
--- a/core/modules/package_manager/src/ProjectInfo.php
+++ b/core/modules/package_manager/src/ProjectInfo.php
@@ -93,9 +93,14 @@ final class ProjectInfo {
    */
   public static function getInstallableReleases(array $available_updates): ?array {
     $releases = $available_updates['releases'] ?? [];
-    $status = $available_updates['project_status'];
+    // The project_status key may not be present if update data hasn't been
+    // fully fetched yet (e.g., during site installation or when update
+    // status data is unavailable).
+    $status = $available_updates['project_status'] ?? 'not-fetched';
+    
     if ($status !== 'published') {
-      throw new \RuntimeException("The project '{$available_updates['name']}' can not be updated because its status is '$status'.");
+      // If the project status is unknown, we cannot determine installable releases.
+      return NULL;
     }
 
     $installable_releases = [];
